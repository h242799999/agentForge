# 规格符合性审查报告

**审查范围**：`{代码路径}`
**对照文档**：`{文档名} — {章节列表}`
**审查时间**：`{日期}`

---

## 规格匹配摘要

| 代码模块 | 对照文档 | 章节 | 匹配状态 |
|---------|---------|------|---------|
| `payment/` | api-design.md | §3 支付接口 | ✅ 已匹配 |
| `PaymentViewModel` | detailed-design/payment.md | §2 状态机 | ✅ 已匹配 |
| `HomeScreen` | ui-spec.md | §5 首页 | ⚠️ 部分匹配 |

---

## 🔴 规格不符（Critical）
> 代码实现与文档明确定义不一致，必须修复后合入

| 文件 | 行号 | 规格要求 | 实际实现 | 修复建议 |
|------|------|---------|---------|---------|
| `PaymentApi.kt` | L42 | 错误码应返回 `4001`（api-design.md §3.4） | 返回了 `400` | 按文档修正错误码枚举 |

---

## 🟠 规格疑问（Major）
> 代码与文档存在偏差，需要与 PO / 设计师确认意图

| 文件 | 行号 | 文档描述 | 代码实现 | 确认问题 |
|------|------|---------|---------|---------|
| `CheckoutFlow.kt` | L87 | 文档描述支付前需校验库存 | 代码中无库存校验 | 确认：此步骤是否应在前端还是后端处理？ |

---

## 🟡 规格遗漏（Minor）
> 文档中定义但代码尚未实现（可能是有意推迟，需确认）

| 规格要求 | 来源文档 | 行号 | 建议 |
|---------|---------|------|------|
| 支付超时自动取消（30s） | api-design.md §3.5 | L180 | 确认是否在本期实现 |

---

## 💡 文档更新建议
> 代码实现超出或优于文档定义，建议将以下改进同步到设计文档

- `PaymentViewModel` 新增了 `PENDING_RETRY` 状态，比文档中的状态机多一个状态，建议更新 detailed-design/payment.md §2.1

---

## ✅ 符合规格的部分

- 支付接口参数（amount、currency、orderId）与 api-design.md §3.1 完全一致
- 认证 Token 格式（Bearer JWT）与 api-design.md §2.3 一致
- 订单状态流转逻辑与 detailed-design/order.md §3 状态机一致

---

## 代码质量问题（通用规则）

> 来自 `review-commons/RULES.md` 的检查结果

| 级别 | 文件 | 行号 | 问题描述 | 修复建议 |
|------|------|------|----------|----------|

---

## 行动计划

### 必须修复（合入前）
1. 修正 `PaymentApi.kt` 的错误码定义

### 待确认（需与 PO / 设计师沟通）
1. 库存校验是否应在客户端处理
2. 支付超时自动取消是否在本期实现

### 文档更新
1. 补充 `PENDING_RETRY` 状态到 detailed-design/payment.md 状态机图

---

*由 spec-reviewer 生成 | Claude Code*
