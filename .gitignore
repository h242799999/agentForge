# ─── 设计文档与生成内容（本地使用，不上传）─────────────────
# specs/ 目录下的实际文档和生成的 INDEX（本地填写，不提交）
specs/**
!specs/.gitkeep
!specs/INDEX.example.md

# reviewer/ 目录下的 review 报告（生成内容，不提交）
reviewer/**
!reviewer/.gitkeep
# ──────────────────────────────────────────────────────────

### macOS
# Finder metadata
.DS_Store

# Thumbnails
._*

# Custom folder icons
Icon

# Volume root files
.DocumentRevisions-V100
.fseventsd
.Spotlight-V100
.TemporaryItems
.Trashes
.VolumeIcon.icns
.com.apple.timemachine.donotpresent